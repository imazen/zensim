# HDR-944 bake wave — pre-registered (2026-08-27); TRAINS NOTHING tonight

MODELS-criterion HDR lane + the svt loop's route-(b) feeder (zenav1-svt
`benchmarks/zensim_hdr_target_wave_2026-08-27.md`, chunk-2 fork). Frozen
before any fit, per WAVE_PLAYBOOK.

## Data (frozen)
`/mnt/v/output/zensim/hdr944-leg/hdr_v3mix944_{train,val}digits_2026-08-03
.parquet` (7,410 / 3,900 × 944; manifested `build_commit b464855a`;
orientation-gated PASS 2026-08-27, +0.849/+0.861; store+Tower mirrored).
Target = `human_score` (the cvvdp-mix — the corpus's carried asset; pure
cvvdp-scalar remains the known dead end). Split = the tables' origin-digit
split, UNTOUCHED.

## Arms (frozen)
- L0: the 2-layer family recipe (the sota944 winner shape) at k=3 seeds,
  `--out-dtype f16` QAT per the standard packing, coarse-decay 1e-5.
- L1: the same + kadis-negrich negative-tail mass at the family weight.
- Baseline row: shipped BHdr (372-class) re-read on the SAME eval set for
  scale (different front-end — stated, never hidden).

## Gates (frozen; owners' tools only)
- `bake_verdict --full-json` per bake (rank + dial panels); selection by
  `freeze_check --select` (E.4: profile floors PRIMARY, balanced+0.15·M3a
  tiebreak; M3a via the standard harvest).
- **UPIQ instrument read MANDATORY pre-ship** (`upiq_panel` /
  `run_external_reads.py --scorer bake:` route) — the hdranch3 lesson.
- Packaging: `bake_dial_refit pack` (prune + f16 + spline-on-packed);
  identity gate bit-exact-or-1e-4 per the standard.
- Ship/freeze/default: USER-GATED, always.

## Endgame
Verdicts + fulleval JSONs on the board via `promote_fulleval.py`; the wave
md carries the table; the svt loop's route-(b) swap becomes proposable
only after the UPIQ read passes.

**AMENDMENT (2026-08-27, before any fit)**: the "trains nothing tonight"
line above was scheduling caution, not a registered gate — the waves rule
requires registration BEFORE runs, which this file satisfies. L0 runs now;
the frozen data/arms/gates are unchanged.

**AMENDMENT 2 (before any fit)**: the winner-family shape per the embedded
W10L9 repro is `--n-hidden-layers 0` (the additive-class finding), NOT
"2-layer" as the arms section mis-wrote from memory. L0 = 0-hidden, epochs
120, pairs-per-epoch 50000, coarse-decay 1e-5, target human_score/100,
seeds {4003,4004,4005}. The SDR argv's per-feature winsor/signed_cbrt list
is NOT copied (those bounds are SDR-corpus percentiles; wrong clamps on the
HDR route) — L0 runs transform-free; a transform-screened arm may register
later with HDR-fit bounds.

## L0 RESULT (2026-08-27, k=3 — first HDR-944 bakes ever)
| seed | best val SROCC (hdr_v3mix val, held-out digits) |
|---|---|
| 4003 | **0.9635** |
| 4004 | 0.9630 |
| 4005 | 0.9622 |
Seed spread 0.0013 — the additive-class stability signature. Bakes at
`/mnt/v/output/zensim/bakes/hdr944-2026-08-27/HDR944_L0_s{4003,4004,4005}
.bin` (+ .spec.json, embedded repro in each; s4003 sha prefix recorded in
the commit). Trainer emitted each with the standard embedded `zentrain.
repro`. NOTE the val number is IN-FAMILY (cvvdp-mix targets, same corpus
family) — it gates nothing; the registered gates follow.

## GATE GAP FOUND (registered before proceeding): UPIQ features are 372-era
`run_external_reads` rescoring uses the STORED pu-linear-372 UPIQ feature
tables — a 944 bake cannot consume them. The MANDATORY UPIQ pre-ship read
therefore needs a **944-regime UPIQ feature extraction** (the hdrpq/hdr100
extractor route over the UPIQ sources) — registered as this wave's next
chunk; NO ship/selection claim is possible until it runs. L1
(negrich arm) also waits on it (no point training arms that cannot be
gated).

## CORRECTION + UPIQ GATE RESULT — **L0 FAILS** (2026-08-27, same session)
First: the "gate gap" section above was WRONG — `upiq_hdr_944.csv` exists
(`/mnt/v/output/zensim/hdr-dmean-2026-07-29/`, 944-regime) and
`run_external_reads --scorer bake:` consumes 944 bakes directly. The gate
ran immediately:

| seed | UPIQ pooled SROCC | narwaria | korshunov |
|---|---|---|---|
| 4003 | **+0.2477** | +0.0773 | +0.3475 |
(recorded probe944 reference: pooled 0.7742, korshunov 0.9346)

**The in-family val 0.96 collapsed to ~0.25 on the independent human-JOD
instrument — the exact failure mode the mandatory UPIQ gate exists to
catch (the hdranch3 lesson, again).** L0 ships NOTHING; the svt loop's
route (b) stays closed; route (a) (shipped BHdr) remains the loop default.

Candidate levers for the NEXT registered arm (analysis before any fit —
none run tonight): (1) the family's winsor guards refit on HDR
percentiles — L0 ran transform-free and the IW-family features are KNOWN
to explode unguarded (the load-bearing-winsor memory); (2) training
breadth — hdr_v3mix is a single-codec (jxl) corpus; Q-G6 named
multi-codec breadth as the lever and the hdr944_mc leg is its registered
data task; (3) target-form study (the cvvdp-mix may not transfer alone).

## L0 FAILURE DIAGNOSIS (measured) + L0w REGISTRATION (before its fit)
Prediction-distribution probe (s4003, canonical forward): on UPIQ **299 of
380 predictions saturate** (248 at ≤0.5, 51 at ≥99.5); on the bake's OWN
val the raw range is [0, 18.3] (p50 3.1) against a 0–100 target — the
in-family 0.96 was RANK-only inside a collapsed band, and out-of-domain
the unguarded features explode through the linear head to the pins,
destroying rank too. **Lever (1) confirmed: the family's winsor guards are
load-bearing** (the IW-explosion memory, re-proven).

**L0w (registered now):** identical to L0 plus `winsor_p99:<idx>:<p0.5>,
<p99.5>` guards for ALL 944 features, bounds fit on the TRAINING table
only (never val/UPIQ). One seed (4003) first; the UPIQ read gates before
any further seeds. Same everything else.

## L0w RESULT — WORSE on UPIQ; wave PAUSES on data breadth (2026-08-27)
L0w (all-944 train-percentile winsor guards): in-family val 0.9643 (best
yet) but UPIQ pooled **+0.1430**, narwaria **+0.0000** — the guards clamp
UPIQ's out-of-distribution features toward constants, killing what little
signal L0 had. Verdict: **not explosion alone — severe DOMAIN SHIFT. A
single-corpus (jxl-only) HDR training table cannot produce a general HDR
metric with any amount of guarding.** (The KADIS full-data lesson's HDR
echo: breadth rescues.)

**WAVE STATE: PAUSED ON DATA — the breadth prerequisite is the hdr944_mc
leg** (B4(v): jxl + svt + gainmap arms over the hdrgrid corpus, origin-
digit split): its inputs are (i) the hdrgrid GPU scores now draining and
(ii) the hdrfeat944 re-declare (the other lane's mis-declared run;
`declare-scorefiles` is the vehicle). When both land, the L1-breadth arm
registers with the same gates. The three L0/L0w bakes stay on disk as the
recorded baseline; NOTHING ships; the svt loop keeps route (a).

**AMENDMENT 3 (2026-08-27, pre-L1): a THIRD breadth prerequisite.** The
gainmap census cross-check (zenmetrics `c537feef`) found hdrgrid's
`zensim_score` column is **judge-era-MIXED** (identical blob 43.57 in the
08-07/08 wsl-c1/c2 era vs 25.13 under the current judge; the `--hdr`
zensim judge is a path dep on the live zensim tree, so each executor
image bakes its build-date model). The hdr944_mc leg's target hygiene
therefore additionally requires the era unification (re-score of the
ts ≤ 08-08 zensim rows under the pinned current image, or era-stamp +
filter — recorded in PLAN_LAN_ERA_REFINEMENT DATA section). L1 does not
register until (i) GPU scores, (ii) hdrfeat944 re-declare, AND (iii)
era-unified zensim targets are all true.
