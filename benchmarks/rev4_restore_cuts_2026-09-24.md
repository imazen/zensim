# Restored-cut bank sidecars (2026-09-25, quarantined)

18 promoted Rev4 bank sets, 248,983 unique pair_keys, four sidecars per set (`features__restore_{mapdev,z1max,gmsnative,dvifmgate}.parquet`, f32 round-nearest-even, zstd-3 BYTE_STREAM_SPLIT, 65,536-row groups, bank key order) at `/var/tmp/restore-cuts/bank/<set>/` with `_MANIFEST_restore.json`. 260.2 MB total (mapdev 37.7, z1max 176.8, gmsnative 33.1, dvifmgate 12.6). Per-set rows, wall, pairs/s, RSS, file sha256 and dead columns: `rev4_restore_cuts_2026-09-24.json`. Pixels only: pair TSVs carry `human_score = 0`; no label or `_sealed/` file was opened. Confirmation sets were read for pixels under ruling D4 (the coordinator's ledger entry, proposed in the DONE file).

Producer: candidate commit `ec5b1821` (clean snapshot of `quarantine/claude/restore-cuts`), extractor sha256 `4ea8f3331fd585ae...`, `ZENSIM_FORMULA_REV=3 ZENSIM_ROOT_FORM=sqrt`, `--restore-cuts mapdev,z1max,gmsnative,dvifmgate`, input contract `legacy-rgb8`, feature set `basic+v2+append+append2+csfw+dvifm+gridblk+ringbasis+tailhist+arttype+gmsbank+mapdev+z1max+gmsnative+dvifmgate@w1825/unknown#eb217aec` (the v1 pools f156..371 are unrequested, hence not computed). Build meta (`build_meta.json`): 15 repositories at fetched mains (list in it), 20 local packages, 220 registry packages, 0 git sources, 0 outside the snapshot. Bit-identity baseline: `main@origin` 26494c8a, sha256 `169b49c6...`.

Acceptance: every pair_key once per sidecar, no extras; audit bound to keys by row_id and both pixel hashes; all values finite; identity rows (`pixels_identical`): `mse_dev` and every registry-`Difference` z1max slot exactly 0, `gmsnative`/`dvifmgate` exactly 0, `hfsq/hfabs src == dst` bit for bit (0 violations in all 18 sets); 200 random rows re-extracted fresh reproduce the stored f32 bit-exactly (64,600 cells, 0 mismatches). Dead columns (all-zero over a whole set): only z1max f1574 (`contrast_inc`, scale 0 X) on the four KonJND sets (`konjnd_bpg_train`, `konjnd_bpg_val`, `konjnd_jpeg_select`, `konjnd_jpeg_terminal`), plus f1612, f1631 on `konjnd_jpeg_terminal` — data-dependent, all other columns live everywhere.

Not done here: a tower mirror (`/mnt/tower` not mounted), and the existing-family columns are not re-verified against the stored bank inside these sidecars (they are not stored); f0..f1501 identity is established by the 2,000-pair gate and the parity tests, see the worklog.
