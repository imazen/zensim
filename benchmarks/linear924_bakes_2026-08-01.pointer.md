# E-LIN linear-924 phase-1 artifacts (pointer)

Campaign: `benchmarks/linear924_phase1_2026-08-01.md` (pre-registration + full results).
Code: zensim main `f1b1cdb6` (fit chain) + `1f1fcf90` (driver) + `24eaaee4` (results).

Block storage (canonical): `/mnt/v/output/zensim/bakes/linear924/`
Tower mirror: `/mnt/tower/output/zensim/bakes/linear924/`

| item | what |
|---|---|
| `grams/*.npz` + `grams/_GRAM_MANIFEST.json` | 13 per-corpus raw-moment Grams (924-wide, f64; gram shas + input-parquet shas in the manifest) |
| `bakes/{M1..M5}_lam*.bin` | 35 raw fit-lasso bakes + 35 `_w.bin` winsorized ship-form bakes, each with a `.spec.json` provenance sidecar (arm, λ, gram shas, code commit) |
| `verdicts/*_w.full.json` / `.verdict.md` | per-cell bake_verdict at the 924 invocation |
| `linear924_grid.tsv` | the full grid table (arm × λ → per-corpus SROCC + dial + corruption + bake sha) |

Selected candidate (frozen sdr25 rule): `bakes/M5_lam2e-3_w.bin`, sha256
`f41131f48132f046febfd1fc1a4517240f8b3db75e27d7a298034d7254ef5c27`.
**NOT promoted to `zensim/weights/`** — the pre-registered gates failed
(G-L1 CID22 0.83158 < 0.8764; G-L2 KonJND 0.45735 < 0.50; G-RANGE 1.468% above-knot).
Profile B remains the linear champion.

Every bake is deterministically re-derivable: `scripts/linear924_grid.sh` with the
sha-pinned canonical-924 inputs reproduces the bakes byte-identically (demonstrated:
independent refit of M1 λ=3e-4 matched the smoke-run sha `bf7af02aefbf…`).
