# Prepared maps and model recovery completion — September 15, 2026

User directive: complete the unfinished map extraction, HDR maps, competitive
model recovery and qualification work. Start at47879fee. Repository fetch/status
receipts live under `/var/tmp/zensim-validation-2026-09-15/`; preserve dirty and
divergent sibling work. No secret holdout access.

Implementation registration: retain basic/peak signals and final statistics in
the existing extraction owner, then consume them after complete model
sensitivities are known. Compare canonical consumed features, scalar outputs,
max/L8 queries, finite moments, tiny/odd geometries and supported SIMD tiers.
Keep the existing two-pass path as a discriminating test/benchmark reference.
Measure pinned scalar/prepared latency and memory on synthetic and admitted
TRAIN images before accepting the optimization.

Native HDR API delta: add
`BakeScorer::prepare_steering_hdr(source, encoding, bin) -> SteeringSession`.
The concrete caller is the native-format benchmark and HDR public spatial
validation; reuse the existing session, model, feature and attribution owners.
The session binds the viewing/input contract once. Retain native PQ16/HLG/linear
precision and declared primaries. Unsupported model/sampling/companion routes
must refuse explicitly. Identity, source binding, stale state after errors and
complete canonical feature/scalar parity are required before encoder use.

Recovery: use the verified complete Rev3 SafeSyn/CID22 TRAIN caches and the
previously admitted source-disjoint human/modern-codec TRAIN packets. Freeze
source-family internal fit/development/calibration assignments before fitting;
identify cross-leg duplicate references, preserving prior reservations. Two
regimes only: fast Y60/H32 and richer basic228/H128; five paired initialization
seeds, disjoint sampler windows,120 epochs ×50,000 draws, existing Rust training,
packing and train-only calibration. Preserve negative/identity anchors and
complete public-surface evaluation. Record failures rather than weakening gates.

HDR supervision must be admitted under corrected `hdr-common-primaries-v2`;
old HDR features cannot be relabeled. Prefer shared SDR/HDR weights, assess
this on TRAIN before freezing. Public EVAL (or published TEST where absent)
may assess only frozen final compositions; disclose historical exposures.
Use the complete registered composite, raw scatter/tail/clumping/saturation,
attainable codec bounds,1/2/3 shots, native spatial/RD and independent HDR judges.
A completed assessment may fail qualification; no missing panel becomes a pass.
