# Claude-era recovery: chronology and consequences, September 14, 2026

The exact-project memory audit is **97/99 files read in full**, with unchanged
source hashes recorded in the [audit](claude_memory_chronology_2026-09-14.audit.json).
This is not completion of the user's broader memory/transcript/document request.
`project_profile_b_hdr.md` and `project_lan_era_refinement_plan.md` remain unread
in full; related project roots, transcripts and repository documents also retain
unfinished scope. Reading historical claims does not reproduce their experiments
or authorize access to their historical protected datasets.

The private cumulative audit supersedes the earlier 30/99 and 87/99 counts.
Its final ten reads comprise nine completed after the 87-file checkpoint and
the storage-migration memory in this continuation. All 99 source identities
were rechecked. File modification time and the opening status paragraph are
not reliable chronology: several memories retain early verdicts below later
corrections or add corrections after an older front-matter timestamp.
One infrastructure source name is redacted in the public audit; its content
hash still identifies the original private source.

A separate local discovery pass found275 potentially related memory files
across20 project roots, including176 outside this exact-project directory.
That pass matches paths/content mentioning Zensim and records private source
hashes; it is discovery, not176 additional full reads or exhaustive coverage
of other machines. `RELATED_MEMORY_DISCOVERY.json` retains the bounded queue.

## Findings that change the next experiment

| Earlier interpretation | Later evidence | Consequence now |
|---|---|---|
| July 19 V2 trainability “KILL” means these features cannot help | `project_feature_v2_program.md` bounds that result to a lab recipe with epoch-zero checkpoints. July 23 replicated MLP recipes with basic156 + V2 (504 inputs) reversed the earlier conclusion; the full720 and linear comparisons behaved differently. | Separate optimization, architecture, data mix and feature availability. Do not label a failed recipe a feature ceiling. Historical wins are not current Rev3 qualification. |
| A bake labelled foldable uses only the foldable feature subset | The July 24 amendment measured significant dependence on IW columns in the supposed foldable resolution winner. | Inspect declared and actually consumed features; names and coefficient counts do not establish compute savings or semantic compatibility. |
| Higher severity-ramp monotonicity means a better HDR model | `project_bhdr_hdranch3_falsified.md`, July 14: ramp rate improved 63.7% to 86.5%, while reported UPIQ-HDR rank fell .7536 to .606. | Keep human HDR assessment separate from ramp/teacher diagnostics. A proxy improvement cannot compensate for missing human evidence. Historical UPIQ paths require current split admission before any use. |
| Spatializable feature mass and exact local gradients imply useful steering | July claims in the V2/924 memories precede the current finite/native measurements. September 14 constrained heads can improve own-score map consistency while worsening native quality ordering. | Retain numerical checks, but require native interventions and actual encoding benefit. Own-score coherence is not independent quality agreement. |
| The strongest seed represents a recipe | `project_ssim2_replacement_bar.md`, September 4 correction: A4b's .8664 composite was best-of-three; the reported mean was .8572. Distillation also mixed raw versus served score units before an owner correction. | Compare complete served ensembles and registered replica distributions. Separate improvements in variance from improvements in mean. Teacher tables need the same composition semantics as serving. |
| Different nearby sampling seeds establish independent replicas | The September 14 robust-pair study proves the legacy seeds can be offsets into one stream. | Preserve old measurements but narrow their sampling-variance interpretation; use verified disjoint draw windows for new comparisons. |
| Zero training/validation weights make an auxiliary group irrelevant | The September 14 TV removal control found that the all-zero validation fallback included every reported group in checkpoint selection. | Use explicit fitting-group selection weights and behavioral removal/repeat controls. Early-stop zero does not imply final-epoch export. |
| Failing native TV development proves the feature set is inadequate | The exported TV ensemble also fails most registered fitting margins and has worse mean hinge than control. | The recipe failed. Feature capacity remains unresolved. Do not immediately launch a margin/weight sweep or global chroma ablation. |

Current evidence for the last four rows is in the
[constraint replay](native_constraints_2026-09-14.md),
[sampling study](native_robust_train_2026-09-14.md),
[signed contributions](native_contributions_2026-09-14.md), and
[TV study](native_tv_train_2026-09-14.md). These links expose the measured limits,
including small TRAIN-development populations and mentor dependence.

## Controlling instructions and ownership

The September 13 TRAIN/EVAL/never-TEST ruling supersedes every historical
touch-once, terminal, checkpoint-on-validation, or reserved-segment recipe.
Current explicit user codec priority is JXL, then expansion to AVIF, JPEG and
WebP; the older AVIF-priority note does not reverse that order. Old AVIF holds
were amended several times; inspect the current codec implementation and pinned
build before applying a historical backend restriction.

The codec-specific search and diffmap owners remain in their codec repositories;
the [reuse audit](diffmap_reuse_audit_2026-09-08.md) and
[current playbook](../docs/WAVE_PLAYBOOK.md) index them. Do not replace these with
another central controller. Target seeds use information available before an
encode; pair features and attainable-bound witnesses cannot leak into seeds.
Representability depends on image, codec configuration and chroma mode.

`feedback_keep_launching_review_sets.md` records that the user reviews results
through the gauntlet. Each completed generation therefore needs discoverable
comparison evidence, not only a terminal summary. Current no-delegation,
no-new-worktree, capped-local-execution and safe-push rules supersede older
session-specific operational recipes. The storage memory itself corrects an
initial CUDA diagnosis to stale image/network-client behavior: verify current
processes and pinned binaries rather than treating historical incidents as live.
Private fleet details remain outside the public audit.

## Comparison-page repair

The nine frozen MT913 candidates already have six measured human/codec panels
in `2026-09-14-rev3-codec-eval`; they are not all missing evaluation. Their
composite remains incomplete, and none is qualified. Later TRAIN-development
studies had complete separate reports but no entries in the discussion registry.

Five append-only `role: train-development` entries now link the native-local,
robust-pair, constrained-head, signed-contribution and TV comparisons. The
existing gauntlet renders these in a labelled TRAIN section, newest first,
separate from the EVAL row-filter dropdown. Their failed or incomplete verdicts
are visible beside the links. No model row, metric, or qualification is invented.
The minimal/wide button's stale KADID-only tooltip is corrected to the existing
six-panel coverage. Existing report owners retain all numerical assessment.

The browser harness checks attached report links and prevents TRAIN reports
from appearing as EVAL row filters. Publication verification and hashes are
retained in the private `board-recovery/` evidence directory. Report regeneration
uses stored verdict outputs; it performs no fitting, calibration, scoring,
feature extraction or new protected-population access.

Verification: all **546 existing board rows are exactly unchanged**; only the
embedded discussion metadata differs. All page gates pass, including strict
JSON for563 stored verdicts and the new attached TRAIN-link checks. Script lint
checks615 scripts; Clippy passes. Chromium renders the page successfully. The
published board and all five report destinations return HTTP200 with bytes
matching their local artifacts. This verifies discoverability, not model quality.

The next feature question remains the signed coarse-B phase/filter response
on the exact saved TRAIN pixels. The contribution diagnosis establishes an
unexpected response, not its cause. That diagnosis should distinguish sampling,
feature normalization and learned sensitivity before changing the feature set.
Full targeting, native RD, corruption, runtime and EVAL qualification remain open.
