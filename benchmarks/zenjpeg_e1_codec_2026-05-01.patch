--- a/examples/generate_zensim_training.rs
+++ b/examples/generate_zensim_training.rs
@@ -1041,6 +1041,18 @@ fn build_codecs(filter: &Option<String>) -> Vec<Box<dyn Codec>> {
     let mut codecs: Vec<Box<dyn Codec>> = Vec::new();

     codecs.push(Box::new(MozjpegCodec::with_effort(Subsampling::S420, 4)));
+    // zenjpeg-420-e1: jpegli AQ baseline + sequential + no Huffman optimization.
+    // Fastest zenjpeg config — used to densely fill the q-grid in the
+    // operationally-critical SSIM2 [0, 60] band cheaply (per the
+    // 2026-05-01 low-quality improvement plan in zensim/benchmarks/).
+    // Distinct codec entry so the per-codec breakdown sees it as its own
+    // group and `--dataset-weights "zenjpeg-420-e1:0.5"` can rebalance
+    // the JPEG-family share at training time.
+    codecs.push(Box::new(ZenjpegCodec::with_effort(
+        Subsampling::S420,
+        false,
+        1,
+    )));
     codecs.push(Box::new(ZenjpegCodec::with_effort(
         Subsampling::S420,
         false,
