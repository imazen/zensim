# KonFiG-IQA overlap audit — G-L1 + G-L2 record (2026-08-05)

Registered: `benchmarks/sota944_campaign_2026-08-03.md` APPENDIX L (§L.2, §L.3),
pre-registration commit `e93eba04` (pushed before any screen ran).

## Verdict

**G-L1 (Konstanz / KonJND overlap): CLEAN PASS.**
**G-L2 (T0-holdout screens): CLEAN PASS — 10/10 sources survive, zero excisions.**

- Exact screen (decoded-RGB8 sha256): **0 hits** in all five sets
  (KonJND 1,008; CID22-49; CSIQ 30; LIVE 29; AIC-3 originals 10).
- dHash-64 perceptual screen (`check_holdout_overlap`, the canonical owner,
  threshold 10): **zero d ≤ 10 flags in any set**. The **global minimum Hamming
  distance is 17** (SRC01 vs KonJND SRC0614) — even the d ≤ 16
  screening-for-eyes band is EMPTY, so no montages and no user-review queue
  exist for this audit. Per-set minima: KonJND 17, CID22 20, CSIQ 18, LIVE 19,
  AIC-3 24.
- Registered decision rule outcome: no confirmed overlap with
  `ext_konjnd_jpeg_val` (504) or `konjnd_bpg_val` (101) refs ⇒ the leg is NOT
  dead; no T0 overlap ⇒ no per-source excision.
- The DATA_SPLITS.md:152 "dHash spot-audit pending" item is now RUN (this
  record).

## Files

| file | content |
|---|---|
| `dhash_konfig_vs_{konjnd,cid22,csiq,live,aic3}.tsv` | per-KonFiG-ref nearest holdout ref + Hamming distance (tool output verbatim; column header says `cid22` for the refs column because the tool predates multi-corpus use — the refs dir is the named corpus) |
| `exact_screen_konjnd.tsv` | per-ref decoded-pixel sha256 + exact-match column (all `-`) |
| `exact_screen_holdouts.tsv` | per-holdout-set exact-hit counts (all 0) |

## Provenance

- Tool: `check_holdout_overlap` built from this workspace
  (`CARGO_TARGET_DIR=$HOME/tmp/zensimkf-target`, release), workspace parent
  `532e3a1f` + appendix-L commit `e93eba04`.
- KonFiG refs: `/mnt/v/dataset/konfig-iqa/KonFiG-IQA/IMAGES/reference_images/`
  (10 files, 384×512).
- Holdout ref dirs: KonJND `/mnt/v/datasets/KonJND-1k/KonJND-1k/source_image`
  (1,008 @ 640×480); CID22 `/mnt/v/dataset/cid22/CID22_validation_set/original`
  (49); CSIQ `/mnt/v/dataset/csiq/*.png` (30); LIVE
  `/mnt/v/datasets/LIVE/databaserelease2/refimgs` (29 BMP, staged as lossless
  PNG at `/mnt/v/output/zensim/konfig944/audit/live_refs_png/` for the
  png/jpg-walking tool — dHash operates on decoded pixels, so staging is
  identity); AIC-3 `/mnt/v/dataset/aic3_ctc_epfl/original` (all 10 originals —
  a superset of the 5 refs the aic3/aic4/sdr25 subjective sets use).
- Contact sheet for the user (registered L.2 mitigation, informational since no
  flags): `/mnt/v/output/zensim/konfig944/audit/konfig_10refs_contactsheet.png`
  → http://localhost:3300/zensim/konfig944/audit/konfig_10refs_contactsheet.png
- Registered residual (L.11.8): dHash-64 does not catch crops; the
  KonFiG(384×512 portrait) ↔ KonJND(640×480 landscape) geometry means a
  shared-KonIQ-pool crop relationship would evade it. No screen here claims
  otherwise.

## Dedup-rule verification (L.6 reproduction gate, run as groundwork)

The frozen enumeration+dedup rule (PartA alphabetical×levels, then PartB;
per-source decoded-pixel-sha dedup, keep-first) reproduces the 2026-07-02
372-era table EXACTLY: 1,220 enumerated → 1,090 kept (850 PartA + 240 PartB);
`(ref_basename, q_jnd)` multiset equality: **True**; max
|July `human_score` − (1 − q_jnd/3.2)| = **0.0**; kept identity rows = exactly
10 (one per source: PartA colordiffusion level 0, first in enumeration order).
Pairs (written by the committed builder, superseding the groundwork staging):
`/mnt/v/output/zensim/konfig944/build/konfig_pairs.tsv` (with per-row
source/part/distortion/level/q_jnd + dist-pixel sha256).

## Orientation groundwork (G-L4, determination half)

- EXP_III raw votes: 910 stimuli × 75,519 votes (exactly the readme count),
  keyed (Source, Distortion Type, Distortion Level); Answer ∈ [0, 4].
- Cross-foot vs the distribution's own `scores.csv`: **910/910 stimuli, 0
  mismatches** on both mean and n_ratings.
- DCR direction: mean(level-max) > mean(level-0) in **70/70**
  source×distortion ladders ⇒ the raw DCR scale is degradation-oriented ⇒ the
  stored `human_score = 1 − q_jnd/3.2` (falling in level) is QUALITY-oriented,
  matching the declaration to be registered in `check_target_orientation.py`.
  The formal signed-SROCC gate runs against the built 944 table.

## Build + remaining gates (appended 2026-08-05, post-build)

- **Extraction**: `ZENSIM_AB_MODE=foldapp2 v2_ab_extract` (this workspace,
  build_commit `e31de496`), 1,090 rows × 944, 59.2 ms/pair.
- **G-L5**: 8 konjnd_bpg pairs re-extracted at this rev vs stored canonical
  `konjnd_bpg_train_944.parquet`: **7552/7552 feature cells exact-equal**.
- **Promote**: `konfig_944.parquet` (1,090; sha `a5bde4d075c13425…`) +
  `konfig_originsplit_{train,val,test}_944.parquet` (327/436/327) +
  `_MANIFEST_konfig.json` at the ext944 root. Triple-mirrored (R2
  `s3://zentrain/ext944-canonical-2026-08-01/` + Tower
  `/mnt/tower/output/zensim-ext944-canonical-2026-08-01/`), sha
  round-trip-verified (local == R2 == Tower == manifest).
- **G-L4 orientation** (`orientation_gate.txt`): **OK** — signed SROCC
  **+0.564482** vs the 75,519-vote raw DCR ground truth (n=850 PartA rows;
  PartB excluded by part, not by key-miss — the first gate run came back n=950
  because PartB rows at level ≤ 12 falsely matched PartA motionblur DCR keys;
  fixed in `konfig_ground_truth_keyed` before any table was consumed).
  Measured quality, declared quality.
- **G-L3 integrity** (`integrity_gate.txt`): A1/A2/A5/B1/B4/B4b/C1 OK;
  **C4 leakage OK** — zero konfig references in any of 15 eval corpora
  (single-table `--leak-eval-root` mode added to the owner for exactly this
  new-leg shape). **One FINDING stands, with a measured disposition**:
  B2 reports 39 constant columns outside the structural-zero block
  (f720, f721, f754–772, f805, f806, f822, …) — the identical 39-column set is
  constant in the existing canonical `konjnd_bpg_train_944.parquet` (sets
  compared: equal, zero symmetric difference). This is the appendix-G
  "never-populated on the SDR route" class, a regime-wide property, not a
  KonFiG defect. The FINDING is left standing (no check is relaxed); this
  disposition is the record.
- **Probe driver**: `scripts/konfig_probe_seed.sh` echo-verified
  (`echo_verify_2026-08-05.txt`): vs L9 exactly (1) the appended
  `konfig:<table>:{0.25|0.75}:0.0:rank` group pair and (2) the `--out` value;
  167 → 169 tokens. Loss-mode `rank` follows the kadid/tid human-label
  convention (the JND-grid unit is not the score unit) — stated here because
  the L.7 registration wrote the group token without the loss-mode field.
