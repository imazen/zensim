# FEATBANK POTENTIAL worklog continuation C — 2026-09-24 UTC

**POTENTIAL — ceiling, not a model score.** Cwd for commands here: `/home/lilith/work/zen/zensim--featbank-potential`. Prior commands, source hashes and exposure boundaries are in `featbank-potential_WORKLOG.md`, `_2026-09-24.md`, and `_2026-09-24b.md`. All fit outputs remain under `/var/tmp/rev4-featpot/`.

## First new registered MLP fit

- The existing idempotent wrapper's `run_mlp_baseline.sh` started KADID TRAIN R0 H32 outer0 rep1 at `2026-09-24T04:27:39Z` and ended it at `04:34:25Z`; the fit command is recorded verbatim in `/var/tmp/rev4-featpot/mlp_kadid_train_r0_h32_0_r1.log` (SHA-256 `e997c2e76bd4ce9f185378ff20f30487614e4ee59861ebd54dc1b0f8da7e867f`). It ran through `/home/lilith/tmp/devin/heavy --mem 16G --jobs 8`; wrapper final line `run-heavy: done rc=0 405s`, peak RSS `0.23GiB`. Its `/var/tmp/rev4-featpot/fits/POT_kadid_train_r0_mlp32/o0_r1/result.json` SHA-256 is `7e5e42e9b980462426c25c8db60444b676155895463b7cdaa548d56609fe8a48`. Exact final result line has selected epoch `5`, `n=1000`, SROCC `0.9467755589739246`, PLCC `0.9469087515178674`, KROCC `0.8008271622291682`, and PWRC `0.9894603073077184`. These are one outer fold and one seed; no family/grid estimate is inferred. The runner then started that replicate's outer-fold importance at `04:34:25Z`.

## Report wording and gates

- At `04:35Z` in this cwd, `ruff check scripts/rev4_featpot/finalize_core_reports.py` printed `All checks passed!`, and `PYTHONPYCACHEPREFIX=/var/tmp/rev4-featpot/pycache python -m py_compile scripts/rev4_featpot/finalize_core_reports.py` exited 0. The guarded future report now says candidate arms have not run and requires reviewed Part B/D1/D2 coverage to be checked at publication time; it does not assume the sidecar review will still be absent then. It describes MCL-JCI as excluded from this baseline pending any later D3 role decision. Source SHA-256 `0cde4c9f6f76a3491ffbb770930b50b26ae511d1d636f898d6b71f4c3a066dc9`.
- A read-only `test -e` gate check at `04:35:30Z` found `CODEX_QUOTA_STOP.md` absent, `GMSBANK_DONE.md` present, `REVIEW_GMSBANK.md` absent, and Part B completion/review files absent. Candidate and GMSD fits remain closed. No held-out `pairs/` or `raw/` label copy has been opened.

## HANDOFF — autonomous baseline stages in progress

- Session 77995: `/var/tmp/rev4-featpot/resume_when_quota_clears.log`; deterministic CI, sham and E1 batches are complete, and the registered 480-cell H32/H128 grid is running. It reacquires `~/tmp/devin/heavy` per fit and per outer importance call, skips saved results, and pauses on a renewed quota stop or low `/home` space. At `04:35:30Z`, only three fit results were known (two old and one new); the first new fit took 405 seconds of actual heavy time. This is a long serial grid. Do not start a duplicate runner.
- Session 71898: `/var/tmp/rev4-featpot/run_mlp_ci_after_grid.log`; waits for the grid success marker, then computes B=2,000 reference CIs for the 480 saved MLP replicates and checks all core receipts. The AIC-3 CI pilot has already passed.
- Session 80254: `/var/tmp/rev4-featpot/run_mlp_aggregate_after_core.log`; waits for the core success marker, then pools the five registered outer-fold predictions per seed for each of 32 set/arm/head cells through `panel`, with reference CIs and separate init/sample-order spreads.
- Session 12538: `/var/tmp/rev4-featpot/run_finalize_after_aggregates.log`; waits for the pooled success marker, verifies source-fit hashes and every registered count, backs up the existing reports under `/var/tmp/rev4-featpot/report_snapshots/`, then updates both external `FEATBANK_POTENTIAL_BASELINE_DONE.md` and `FEATBANK_POTENTIAL_DONE.md`. It retains MLP/peer LODO, JPEG residual and candidate work in MISSING. If an upstream batch fails, inspect its progress log and resume the idempotent script; downstream watchers will remain waiting.
- The baseline adapter currently selects `/var/tmp/rev4-featbank/bank/`; if Part B changes a pinned manifest, a fresh cell process will select the 72 MB, nine-set, hash-audited `/var/tmp/rev4-featpot/bank_snapshot/`. Every label read remains through the role-allowed bank labels file. The current external reports are honest progress snapshots, not an earned potential verdict. Nothing has been packed into weights or pushed.

## 2026-09-24 06:09–06:22 UTC — reviewed P2 accelerated

- Read `REVIEW_GMSBANK.md` **before** opening `/var/tmp/gmsbank/peer_gmsd/`; its verdict is `PROMOTE WITH CORRECTIONS` and the independent check covers exact peer GMSD/GMSM (18 sets, 248,983 unique keys, 249,227 stimuli, 0 mismatches). Reviewer notes that the peer root-bank manifest hash is stale after the bank metadata rewrite but every per-set key hash still matches. The current peer `_MANIFEST.json` SHA-256 is `8d2799c8a9ebaa3f25fa7518b3d7591ee7742b696d7f5592b9ace9974d0d27fc`; `verification.json` SHA-256 is `5429a455120bd41a0c729514e61670c124b85be576dc2fc49a8a9cf5bf695f5f`. P1/P3 still await reviewed Part B sidecars.
- Before inspecting any P2 score, added the dated P2 gate, raw-column order, key-level within-reference joint permutation and BVLS sign-mask rule to `benchmarks/rev4_featpot_prereg_addendum_2026-09-23.md`. Committed as `bf55356bbfcce9dc2ff8b7e8652b06b4bf9cbcd6` at **2026-09-24 06:13:54 UTC** (local bookmark moved). Both peer columns are free in BVLS: the existing TSV pins only f0–f371, and its format has no negative pin for distortion-oriented raw GMSD. This is a diagnostic peer arm, not a shipped feature.
- Added `p2_data.py` with SHA-pinned review receipts, source bank-key hash checks, score identity per duplicate `pair_key`, row-preserving keyed joins, and the preregistered joint permutation. A lightweight test on KonFiG TRAIN returned 327 rows / 327 keys / zero missing values for both P2 and its permutation. No `pairs/`, `raw/`, `_sealed/`, MCL-JCI, or forbidden target was read. Added D1 linear/BVLS, D2 LODO, and paired B=2,000 reference bootstrap drivers using the Rust Gram/fit and `panel` statistic owners; committed as `dd1747b847cf7998cc63f3bd53a62d43d85207cf` at **06:18:38 UTC**. `ruff check`, `py_compile`, and shell syntax checks passed.
- Standalone exact peer row ran under `~/tmp/devin/heavy` after that prereg commit, with all nine admitted D1/D2 sets and B=2,000 reference CIs. Receipt `/var/tmp/rev4-featpot/p2/standalone.json` SHA-256 `c67b8dab1749edf90d56940624863e5d4a312f532b158415b570f5a771b16da5`; command log SHA-256 `640c27c7e48d97109f0c883a9860a55493d150ab893c86d284512abc80dcc0ad`. Against human quality, standalone −GMSD SROCC is 0.814817 on CID22-A (MCOS/100), 0.783043 on AIC-3, 0.844128 on KADID TRAIN; on KonJND BPG VAL it is 0.987234 against **SSIMULACRA2 oracle/100**, not humans. `p2_report.py` wrote an early P2 section including all nine rows to `/home/lilith/tmp/zensim-paper/rev4/FEATBANK_POTENTIAL_DONE.md`, SHA-256 `75b5a6ffa22b812335a58e1763a28b45c930da6bc562419547aaee81f6145ed0` at 06:20:56 UTC. The section explicitly withholds D5 interpretation.
- First P2 BVLS pilot on KonFiG TRAIN, through heavy, completed 5 reference-grouped outer folds plus full fit: nested SROCC 0.841672; full-fit 0.997538. Result `/var/tmp/rev4-featpot/p2/d1/POT_konfig_train_p2_bvls/result.json` SHA-256 `91a7a033bc58266b1c2a6fab3496c2ff977a251d32e17267e8c1cd5ee0d3f277`; command log SHA-256 `24e087267cb4aa5c31731f825518014b3750960bbb97286d170a367475c3a71b`. These are single-arm diagnostics, not P2-vs-P0 evidence.
- At 06:21 UTC, session 41203 started `run_p2_deterministic.sh`, logging `/var/tmp/rev4-featpot/p2_deterministic_runner.log`. It runs 8 source/view sets × P2/permuted × BVLS/lasso D1, then 14 human-set D1 paired comparisons and both seven-fold D2 transfer panels with paired comparisons. Every fit/stat call reacquires `~/tmp/devin/heavy`; it checks review, quota and ≥20 GB free on `/home`. The existing baseline H32/H128 grid remains active and uses the same lock. A P2 H32 KonFiG pilot was queued separately (session 43569) to test 946-column diagnostic MLP admission. No P2 adoption conclusion is available yet.

## 2026-09-24 06:23–06:31 UTC — P2 continuation wiring

- Committed the early P2 report, first standalone/pilot receipts, persistent deterministic runner, and finalizer protection as `37b1444fc9ac490e802c92a633b7281d9bb2e824` at **06:23:36 UTC**. The baseline finalizer now preserves the marked P2 section when it eventually publishes the 480-fit core report. Session 20093 waits for the deterministic P2 completion marker and refreshes that section.
- Added a dated P2 provenance entry to `/home/lilith/work/zen/DATA_PROVENANCE.md`: bank-944 + raw peer f944/f945 is a quarantine-only 946-column composite with no production feature-set ID; peer scorer, manifest, verification and label roles are pinned. The P2 MLP uses `--historical-replay` with an explicit unqualified diagnostic reason because the trainer's admission mechanism has no registered producer ID for a derived peer column. This is never a shipped bake or model score. A KonFiG TRAIN H32 outer0 pilot began through heavy at ~06:27 UTC; the first two inner fits completed by 06:31 UTC, so provenance admission and 946-column training passed. Final prediction/score remains pending.
- Prepared P2 MLP H32/H128 five-seed Latin-square drivers (fit, outer importance, B=2,000 reference CI, pooled seed estimates and paired P2/P0/perm comparison) and 200-draw P2 lasso stability. All use the pinned bank/peer adapter and the same reference fold/seed manifests as P0. Session 74381 waits for the H32 pilot result before launching the idempotent 480-fit P2/permutation D1 grid through the shared heavy lock; session 24889 waits for deterministic P2 then runs 16 full stability cells; session 23647 waits for both pooled P0/P2 grids then computes 14 paired five-seed comparisons and refreshes the report. These stages may take days at the measured ~6–7 minutes per MLP replicate. No D2 MLP implementation or dial/cost conclusion is claimed yet.
- At 06:27:43 UTC the first P2 KADID TRAIN BVLS cell completed, receipt `/var/tmp/rev4-featpot/p2/d1/POT_kadid_train_p2_bvls/result.json` SHA-256 `228e521dc4af8a25ac7f65d57901fa18cc67ddd3497f98ec708638946c021ea1`; log SHA-256 `84838ca7aa673133ff6bc7980a5bb17b681eab9de1c3c90255189160750e0d16`. The deterministic runner advanced to TID2013; it has no P2-vs-P0 verdict until its paired control receipt exists. Baseline session 77995 continues in parallel on the shared lock, and `/home` free space is 68 GB (rule ≥20 GB).

## 2026-09-24 06:32–06:35 UTC — first H32 P2 receipt

- The KonFiG TRAIN P2 H32 outer0/rep0 pilot completed through heavy at 06:34:48 UTC, rc=0, 416 seconds, peak RSS 0.14 GiB. Result `/var/tmp/rev4-featpot/p2/mlp/POT_konfig_train_p2_mlp32/o0_r0/result.json` SHA-256 `861609910710173dd5b06a9cf595044df572b7027c52d85a9122327670df1924`: 4 inner folds, selected epoch 35, 109 held-out rows, SROCC 0.879008. It is only a one-fold/one-seed wiring result; no P2 gain is inferred. The persistent P2 MLP runner (session 74381) saw the pilot marker and queued its first KADID TRAIN cell at 06:34:51 UTC. P0 H32 grid remained active.
- TID2013 P2 BVLS D1 completed at 06:34:48 UTC: result SHA-256 `5f91148744626cb3f1feb05c76a0969effae8927a557470b124aca8d6d22bd6e`; log SHA-256 `bc5f06eef2e468ce4eb6262dce5b4f9d84eeaec7f634eed1aef02b1474aca5f5`. The deterministic runner next queued KonJND BPG TRAIN (oracle diagnostic). A separate KonFiG TRAIN P2 lasso pilot is queued behind the lock to catch path-fitting defects before the bulk lasso stage.
- The P2 D2 driver now checks every source's raw per-reference Gram sum against its whole-source raw Gram at f64 round-off (`rtol=1e-10`, `atol=1e-9`) before using it. Direct KADID TRAIN and KonFiG TRAIN checks found maxima `1.06e-10` and `1.45e-12` in `raw__S`, respectively. The check is committed before any P2 D2 fit. `ruff check` and `py_compile` passed. The P2 report now identifies the only baseline positive-control-sensitive human cells: KADID TRAIN BVLS/lasso and KADID SELECT lasso. It continues to withhold a D5 verdict.

## 2026-09-24 06:35–06:39 UTC — bounded grid acceleration pilot queued

- Commit `ddb18ce2cd83db12db4ecfd6e571f058c2671fda` at 06:35:35 UTC records the P2 D2 raw-Gram audit and H32 pilot. The live external report was refreshed afterward with the P2 section and a title that says P2 running; SHA-256 `ef666af25720018f1d607a7c0a9064231402c7b50c63dce9b98a858a8e981141` (before later script/report edits).
- Direct serial H32 cells take about 6–7 minutes each (P2 pilot 416 seconds), with 480 P0 and 480 P2/permutation fit cells registered. The host has 32 CPUs; one fit's measured peak RSS was 0.14–0.23 GiB. To preserve the requested grid without multi-day avoidable serial time, `accelerate_mlp.py` selects absent fit receipts from the far end of each grid, excludes any cell already queued by the original serial runners, and launches separate isolated cell commands within one `~/tmp/devin/heavy` reservation. It leaves the original idempotent P0/P2 runners to own importance and CI. A bounded **two-cell** pilot (session 44009; `/var/tmp/rev4-featpot/mlp_accel_pilot.log`) is queued; no parallel pass claimed yet. Session 62280 waits for that pilot's successful heavy marker, then runs up to eight disjoint cells per heavy reservation with quota and `/home`-space gates. Any failure stops the accelerator without invalidating prior individual receipts. The original runners remain live and lock-sequenced.

## 2026-09-24 06:39–06:46 UTC — D2 MLP protocol and queue

- Commit `72a846dd3376a94652f09a2cb6cfb66b017f3edc` at 06:39:14 UTC records the bounded P0/P2 D1 MLP accelerator and its pending two-cell pilot. The original runners remain live; no accelerated result has been claimed yet.
- Before any P2 D2 MLP fit, amended the addendum with the exact six-source inner checkpoint rule, equal source draw weights under the trainer's default uniform sampler, per-source TRAIN q0.001/q0.999 normalization, the two VAL substitutions, seven-fold Latin seed pairing, and panel B=2,000 paired reference CIs. This prereg clarification is commit `9efa4429f87288c4be08977eef5a0f0f7ff6819b` at **06:41:12 UTC**. It adds no label population. The source-level group-weight semantics come from `zensim_mlp_train.rs` and `mlp_train/sampling.rs`: the uniform sampler selects groups via normalized `train_weight`, so six groups each weight 1 have equal draw shares. This differs from the inverse-row weighting needed when summing raw Grams.
- Implemented `p2_lodo_mlp_tables.py` (source-normalized fit tables and zero-target eval tables), `p2_lodo_mlp.py` (one held-out-source replicate), and `p2_lodo_mlp_compare.py` (five-seed paired panel bootstrap). Committed as `3a2851b168d1a73bc10a1766761bceb670df3b54` at **06:44:29 UTC** after `ruff check` and `py_compile`. The P2 946-column table remains an explicitly unqualified historical-replay diagnostic; P0's 944-column table retains its registered feature-set ID. All fit tables, bakes and results stay under `/var/tmp/rev4-featpot/p2/d2_mlp/`.
- Session 83121 queues the three D2 table receipts under one shared heavy reservation; session 98320 waits for all receipts, then runs one P2 H32 KonFiG-heldout/rep0 pilot; session 84166 waits for that pilot and then runs all 210 P0/P2/permuted × H32/H128 × seven-fold × five-seed D2 fit cells with idempotent outputs, followed by 14 paired comparison panels. All stages check label roles via the bank adapter, and the persistent D2 runner checks the review, quota and `/home` free-space gates before each heavy job. No D2 MLP result exists at this timestamp.

## 2026-09-24 06:46–06:48 UTC — D2 parallel pilot staged

- Commit `28c33123a221dd946bb64505c94e4dffe4a399c5` at 06:46:26 UTC records the D2 MLP guarded runner and report integration. A separate D2 batch driver works from the far end of each arm's 70-cell grid, excludes already queued serial cells, and runs disjoint replicates under one shared heavy reservation. Session 88806 waits for the single-fit D2 pilot and then queues a bounded two-cell D2 parallel test; session 98211 waits for that success marker before any eight-cell batch. These are queued, not passed. The D2 serial runner stays live and lock-sequenced.
- The P2 deterministic D1 runner completed KonJND BPG TRAIN BVLS at 06:46:02 UTC: result SHA-256 `dde34d543501f1971abfb204f627c2792937abfce5decbe319d1261b107394f2`, command log SHA-256 `f7150c23b2db72cc9c20b29e63e4e4eea6cf8d92290c68f3c74d84949920a959`. This is an oracle target in /100 units, not a human-label gain. The runner then queued CID22-A(25) BVLS. No paired control result exists yet.

## 2026-09-24 06:49–06:53 UTC — report continuity while grids run

- `CODEX_QUOTA_STOP.md` remains absent, `/home` free space is 68 GB, and the quarantine working copy was clean after commit `cad673cbd73e2936c554880bbeff33fe417f6f47` at 06:48:28 UTC. The baseline, P2 deterministic, P2 MLP, D2 table/pilot, stability and guarded continuation sessions are alive; all substantial fit work remains under the shared heavy lock. The D1 two-fit accelerator is queued behind the active KADID P2 H32 fit and has not yet produced a result.
- Adjusted the future baseline finalizer so its title retains “P2 RUNNING; PART B CANDIDATES WAITING” when it preserves the marked early P2 section. This avoids a stale “candidates blocked” headline after the reviewed peer arm has started. No score or gate status changes.

## 2026-09-24 06:54–06:58 UTC — grid denominator correction

- Audited the registered loops and `summarize.py`: for each of P0 and P2/permutation, eight sets × two arms × two heads × (five outer folds + one full fit) × five seeds = **960** fit and reference-CI receipts; the outer-fold importance count is **800**. Historical 480/400 counts above are arithmetic errors in progress prose, not reduced preregistered grids. The P0 and P2 fit runners and `summarize.py` already enumerated the full grids.
- Corrected `check_core_gates.py` to use the summary's registered expected counts, corrected `finalize_core_reports.py` to verify and publish 960/800, and corrected the D1 concurrent accelerator's stop threshold to 960 for each grid. The 04:27 snapshot in the external `FEATBANK_POTENTIAL_DONE.md` now states the accurate remaining counts: 958/960 fits, 959/960 CIs, and 799/800 importance. No score was recomputed or changed by this correction.
- Restarted only the waiting D1 accelerator shell after editing its stop threshold; the active two-fit heavy pilot and all serial grid runners were untouched. The new accelerator shell PID was 2381435. Its success gate remains the two-fit pilot's `run-heavy: done rc=0` marker. The pilot was still computing its isolated far-end KonFiG VAL H128 full/rep4 P0 and P2-permutation cells at the restart. The report's early P2 section and standalone reference row remain intact.

## 2026-09-24 07:09 UTC — P2 interpretation wording

- Refreshed the early P2 section to reproduce the preregistered conditional interpretation table (P1 ≈ P3 > P0, P2 > P1, neither above P0) and to keep it explicitly gated by paired controls, CIs and D5. P1/P3 still await reviewed Part B sidecars; this is no score or adoption verdict. The refresh reads only saved receipts, not any label source. `ruff check` and `py_compile` passed. External `FEATBANK_POTENTIAL_DONE.md` SHA-256 `8947cb077b3b51a6b3521e2c5bcba3f0ae832cadb052154e0ac6494da6951104` after the refresh.

## 2026-09-24 07:14 UTC — receipt-complete P2 report fields staged

- Extended the receipt-only P2 report renderer to show fixed-order five-seed D1 nested SROCC and D2 held-out SROCC per P0/P2/permutation arm, with means, CIs, gaps and init/sample spreads where the saved aggregate provides them. It now counts the 800 registered P2/permutation outer-importance receipts instead of leaving that field permanently marked missing. These detail tables appear only when paired comparison receipts exist. No new fit or score was inferred. `ruff check`, `py_compile` and a report refresh passed; current external report SHA-256 `c2e7479a9fa9ea53c6062143290847bb7727a1c95469ca7159b6fb3d5fbe8a4e`.

## 2026-09-24 07:21 UTC — bounded two-fit H128 accelerator pilot passed

- The disjoint P0/P2-permutation KonFiG VAL H128 full/rep4 batch completed under one `~/tmp/devin/heavy --mem 16G --jobs 2` reservation with `run-heavy: done rc=0 1631s`, peak sampled RSS 0.15 GiB, and pilot log SHA-256 `04e8176132e03d2bf4ef86a3d71875777b5a6bd484a42d21cbe5b75f607af05f`. P0-minus-basic result SHA-256 `146d5288c72254329d503841678fa621afa687735bcd4f722e795c0b2a10da49`; P2-permutation result SHA-256 `a2345a000967238a12fca080cd97689797393e31d1ddee8d6e0b8a35f647e18d`. These are full-fit wiring cells on different arms, not a paired P2 gain estimate.
- The first eight-cell accelerator batch was queued behind the shared heavy lock at 07:21:12 UTC, with 21/960 P0 and 3/960 P2/permutation fit receipts at that moment. Session 83210 owns the persistent runner log `/var/tmp/rev4-featpot/mlp_accel_runner.log`; the earlier short-lived PID 2381435 did not survive its launching shell and is superseded by this session. The serial P0/P2 grid runners, deterministic P2 queue, and D2 table queue remain alive. The accelerator still stops on quota or `<20 GB` free on `/home`.

## 2026-09-24 07:22 UTC — D2 MLP table gate passed

- The shared-heavy D2 table stage completed with `done rc=0 9s` and log SHA-256 `ca3d94c6c5a9361e093cc69fe1811c88c3b026866910bd879280ffb686c90fce`. The source/evaluation table receipts are P0 `b454925e276817107b7fb802e506238ba3f58117b51843a21a17867db3f1855a`, P2 `9376b1f256e1fa7a10e339c5550836994baf6c64f67658fb2879f4199f4435b3`, and P2 permutation `5a1bf9bf55299ec06ae4ead281d868ca97d089314a940f4b57a100d60faab00b` (SHA-256). Each covers the same nine admitted source/evaluation views. A read of only the **derived** evaluation Parquets' `human_score` columns verified exactly zero nonzero values in all 27 tables; held-out targets are absent from predictor inputs. This verification did not open any forbidden label copy.
- The first P2 H32 KonFiG-heldout D2 replicate was queued after those receipts. It must produce its own result before the serial and two-fit D2 accelerator runners proceed. No D2 MLP score or transfer conclusion is claimed yet.

## 2026-09-24 07:31 UTC — staged D2 report continuity

- The core baseline finalizer previously left H32/H128 LODO marked open even if all D2 MLP panels finished after the core grid. It now conditionally verifies all fourteen P0 D2 paired-panel receipts, their fixed five source-result hashes, bootstrap protocol and two VAL substitutions, then prints a seven-fold H32/H128 P0 table with five seeds, mean, min/max and reference CI. It reads saved receipts only. The baseline title and MISSING text change only after the full D2 panel gate is met; incomplete D2 stays explicitly open.
- Added a guarded report refresher that waits for both `CORE_REPORTS_UPDATED` and the D2 `compare_done` marker, checks quota and ≥20 GB free on `/home`, reruns the verified finalizer, and refreshes the marked early P2 section. `ruff check`, `py_compile`, `bash -n`, and a no-D2-present probe of the conditional report path passed. No D2 score exists yet; this only stages final publication after the queued jobs complete.

## 2026-09-24 07:32–07:35 UTC — live report and BVLS sign evidence

- Session 41963 (`/var/tmp/rev4-featpot/run_finalize_after_d2.log`) is the guarded post-D2 report watcher. Its first line is `WAIT D2/core reports 2026-09-24T07:32:00Z`; it waits for both success markers and does no heavy work. `run_finalize_after_d2.sh` and the conditional P0 D2 report path are committed as `330474488635c677cd1c7d99108e6830555ccc7d` on the local quarantine bookmark.
- The first eight-fit D1 accelerator batch started under a single `~/tmp/devin/heavy --mem 16G --jobs 8` reservation. Its live `/var/tmp/rev4-featpot/mlp_accel/batch_0001.log` selected four far-end P0 and four P2-permutation KonFiG VAL H128 full/rep0–3 cells. At 07:33 UTC, eight trainer children used about eight CPU cores and the heavy scope used 1.25 GB, below its 16 GB cap; the batch had no completion receipt yet. This is an operational sample, not a performance claim.
- CID22-A P2 BVLS D1 result completed: `/var/tmp/rev4-featpot/p2/d1/POT_cid22_a25_p2_bvls/result.json` SHA-256 `c3cd6373fc594ff4ac7da968878d63ab4159183791f13d875b9180471e90974a`, log SHA-256 `428f2d5953129f2640a02a89713b7e0918669d7dc2ec76197f06fdd167c61d41`. The D1 runner advanced to AIC-3. No paired P2-vs-P0 estimate exists yet.
- The receipt-only P2 report now prints each completed BVLS set's full and five outer-fold standardized weights for the two explicitly free peer columns, and will print D2 BVLS peer coefficients when that panel exists. The refreshed external report SHA-256 is `9bc2c9fbad0b5f77fe89a2c9c1ca570e3174b5b41375d41cc2f4ccf92103316b`. These weights are descriptive, and no sign or family conclusion is inferred. `ruff check`, `py_compile` and the report refresh passed.

## 2026-09-24 07:37 UTC — D5 cost scope and D2 table width

- A footer-only audit of all 48 derived D2 MLP source/evaluation Parquets found 16 tables per arm, all with the expected 944 P0 or 946 P2/permutation feature columns. This supplements the prior 27 zero-target evaluation-table census; no new target value was opened for this width audit.
- The early P2 report now explicitly states that Rev4 plan §2.4 has numeric extractor budgets for C1–C4/all-four but no numeric budget for the separate exact peer scorer in diagnostic P2. The measured C8 `gmsbank` extractor cost cannot be substituted for P2 peer cost. Therefore the D5 P2 cost gate remains unpassed and no adoption verdict may be claimed from these fits. `ruff check`, `py_compile` and receipt-only report refresh passed; external report SHA-256 `9040ce0f7878d7b3744fc5757a1669ecbba57a99893934dc1bef45b9c2c734f7`.

## HANDOFF — guarded long-running gates at 2026-09-24 07:38 UTC

- `FEATBANK_POTENTIAL_DONE.md` exists as an **in-progress** report, with the early reviewed P2 section, standalone −GMSD/+GMSM rows, the conditional interpretation table, descriptive BVLS peer coefficients, accurate MISSING counts and the explicit unpassed D5 cost gate. It is not a final potential or adoption verdict. `FEATBANK_POTENTIAL_BASELINE_DONE.md` is also still a progress report. Nothing was pushed or packed into weights.
- P0 baseline serial grid: session 77995, `/var/tmp/rev4-featpot/resume_when_quota_clears.log`; CI/core/aggregate/finalizer followers are sessions 71898, 80254 and 12538. At the 07:36:27 UTC census, P0 had **21/960** fit receipts. The P0 deterministic CIs, shams and E1 cells are complete. The corrected core gate demands 960 fits/CIs and 800 outer importance receipts, with 32 pooled five-seed rows afterward.
- P2 deterministic D1/D2 runner: session 41203, `/var/tmp/rev4-featpot/p2_deterministic_runner.log`, at AIC-3 P2 BVLS after **6/32** D1 fit receipts. Its comparison and stability followers are sessions 20093 and 24889. The reviewed peer manifest/key join and 9-set standalone receipt passed. No paired D1/D2 comparison exists yet.
- P2 D1 MLP serial runner: session 74381, `/var/tmp/rev4-featpot/p2_mlp_runner.log`; first H32 pilot and KADID outer0 succeeded. The two-fit H128 accelerator pilot passed; the corrected 960/960 P0/P2 concurrent accelerator is session 83210, `/var/tmp/rev4-featpot/mlp_accel_runner.log`, with the first eight-fit batch active under one shared heavy reservation. P2 had **3/960** fit receipts and **1/800** outer-importance receipts at the census. D1 five-seed paired comparison follower is session 23647. The accelerator takes disjoint far-end cells; serial runners retain CI/importance ownership.
- D2 MLP P0/P2/permutation table receipts passed and all 27 eval tables have zero target input. The single P2 H32 KonFiG-heldout pilot is queued as session 98320; D2 serial/paired runner 84166 waits on its result, while the two-fit/eight-fit D2 accelerators wait as sessions 88806/98211. D2 fit receipt count was zero at the census. Post-D2 report session 41963 (`/var/tmp/rev4-featpot/run_finalize_after_d2.log`) waits for both the core report and all fourteen paired D2 panels, then refreshes the baseline report and preserves/refreshed the P2 section.
- All heavy fits use `~/tmp/devin/heavy`; `/home` had **68 GB** free, above the 20 GB rule; `CODEX_QUOTA_STOP.md` was absent. The shared lock had other lanes queued. Continue by inspecting the named logs and idempotent receipts; if a runner exits with an error, fix its exact failed cell and resume its idempotent script through heavy. Part B candidate/P1/P3 arms still await their reviewed sidecars and the coordinator's later message. MCL-JCI remains out pending D3. Do not claim D5 or full baseline completion from this snapshot.
