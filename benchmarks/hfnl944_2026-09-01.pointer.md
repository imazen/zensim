# hfnl944 — near-lossless clause artifacts (2026-09-01)

Block storage: **`/mnt/v/output/zensim/hfnl944-2026-09-01/`**
(`_MANIFEST.json` carries `build_commit`, per-file sha256, row counts, the
NOT-REACHABLE record, and the registered axis definition).

Written up in [`ssim2_replacement_bar_2026-08-31.md`](ssim2_replacement_bar_2026-08-31.md)
**APPENDIX A**. Nothing here is >30 KB-in-git material; the repo holds only the
four scripts under `benchmarks/ssim2_bar_2026-08-31/`.

| file | what it is |
|---|---|
| `hfnl_reachability.json` | the two findings from stored bytes: `human_score == ssim2_gpu/100` exactly on 1200/1200 rows (ssim2 SELF-TARGET), and the 1,200 distorted JXL bitstreams NOT-REACHABLE |
| `peer_hfnl_val.txt` / `peer_hfnl_train.txt` | `panel --per-group` with ssim2 as the predictor on `hf_nearlossless` — pooled SROCC **1.0000**, per-ref mean 1.0000 (48 / 148 refs). The measured proof of circularity |
| `cid22_hfnl_band_panel.json` | the non-circular axis `hfnl_cid22band` (CID22 MOS ≥ 0.80, n=1425 / 49 refs) — pooled + per-ref statistics for all six arms |
| `cid22_hfnl_band_paired_boot.txt` | reference-clustered paired bootstrap on that band, B=10000 seed 20260901, with the sign + min-draw guards |
| `paired_boot_default_reproduction.txt` | gate 7 — the exam's §3.1/§3.2 numbers, unmoved by the band extension |
| `band_<arm>_cid22hi.tsv` | the six per-arm band tables the panel binary read |
| `pp_Q7b_cid22.tsv` | Q7b's per-pair dump in the exam lane's shape (built from its board cell after an exact target-identity gate) |
| `graftsrc/` | the six same-bake source verdicts handed to `promote_fulleval.py --graft-rank` |

**Not mirrored to R2/Tower** — every input is already triple-mirrored and every
output is regenerable in ~4 minutes from the reproduction block in A.8.
