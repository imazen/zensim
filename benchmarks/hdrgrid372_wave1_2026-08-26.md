# hdrgrid372 wave-1 — REGISTERED before any run (2026-08-26)

First MLP wave on the hdrgrid372 cvvdp-mix leg (new lineage; see
`benchmarks/hdrgrid372_linear_baseline_2026-08-26.md` for the floor).

## Arms (frozen)
{H64, H128} × seeds {1, 2, 3} = **6 runs**, sequential (machine-safety: never
concurrent heavy jobs). Fixed argv for every arm:

```
zensim_mlp_train \
  --group hdrgrid372:/mnt/v/output/zensim/hdrgrid372-leg/hdrgrid372_v3mix_traindigits_2026-08-26.parquet:1.0:0.0:both \
  --group hdrgrid372_val:/mnt/v/output/zensim/hdrgrid372-leg/hdrgrid372_v3mix_valdigits_2026-08-26.parquet:0.0:1.0:both \
  --hidden {64|128} --seed {1|2|3} --epochs 200 \
  --out /mnt/v/output/zensim/bakes/hdrgrid372-probe/wave1_h{H}_s{S}.bin
```
(max-features = trainer default 372 = leg-native width; every other flag at its
default; the group syntax mirrors the campaign hdr-leg precedent.)

## Gates (frozen before runs)
- **G-W1**: best arm val SROCC (via `scripts/hdrgrid372_val_read.py`, the same
  owner read as the floor) **> 0.8155** (linear floor 0.8105 + 0.005 margin).
- **G-W2**: every member's val SROCC positive (orientation sanity).
- **G-W3**: report the per-arch seed spread (max−min); no gate value — this leg
  has no prior spread measurement, so wave-1 MEASURES it (never extrapolate).

## Endgame (frozen)
`hdrgrid372_val_read.py` on all 6 bakes → results table appended BELOW this line
in this same file (one terminal file), gates evaluated, memory updated. Bakes
persist under `/mnt/v/output/zensim/bakes/hdrgrid372-probe/` (persist-everything).

---
