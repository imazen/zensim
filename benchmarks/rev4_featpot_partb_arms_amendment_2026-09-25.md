# Rev4 POTENTIAL amendment — Part B candidate arms finalised (2026-09-25)

**POTENTIAL — ceiling, not a model score.** Completes the candidate preregistration. Supplements `d2169f5b`, `044f00dc`, `bf553563`, the C1-C4 amendment, the cost amendment `61d3c07a050d`, the csfw/C7/B1 amendment `28daeec17d59`, the restore-cuts amendment `72b35b43439b` / pin `c56290c10f13` and the identical-pair amendment `f569b10c21cd` (2026-09-25T16:27:40Z). Written 2026-09-25T16:40:28Z by Claude Sonnet lane `featbank-potential`, **before any Part B arm result was read.** (The eight voided restore `a1` cells are disclosed in the identical-pair amendment and stay void.)

## Inputs (final; label-free pin)

Coordinator: Part B is promoted and landed (zensim `e6c9b630`). Sidecars under `/var/tmp/rev4-featbank/bank/<set>/`: `features__rev4c1c4.parquet` (f986-f1321), `features__csfw_dvifm.parquet` (f944-f985), `features__gmsbank.parquet` (f1322-f1501, C8 revised), joined strictly by `pair_key` to the admitted Rev3 tables (a missing or duplicate key fails the arm). Per-set manifest, keys and sidecar sha256s, row and unique-key counts and identical-key counts for the nine D1/D2 populations are pinned in `benchmarks/rev4_featpot_partb_arms_2026-09-25.json` (sha256 `9cfaecd402b628113e4f18115957e9d21b81b1f66ede7d19dd45ee3be1b302a9`), generated by `scripts/rev4_featpot/partb_arms_pin.py` from manifests, parquet schemas and the `pixels_identical` flag only. The Codex-era candidate pin flow (`candidate_pin.py`, report-hash gate) is superseded by this pin; the load-time check is `restore_data.preflight`.

## Arms (each on R0 = Rev3 944, each with a size-matched permuted control)

| arm | added columns | source |
|---|---|---|
| c1, c2, c3, c4 | f986-1081 (96), f1082-1153 (72), f1154-1297 (144), f1298-1321 (24) | rev4c1c4 |
| all | C1-C4 together, f986-1321 (336) | rev4c1c4 |
| csfw | f944-955 (12) | csfw_dvifm |
| c7 | f956-985 (30, DVIFM curve form) | csfw_dvifm |
| p1 | gmsbank f1322-1501 (180) | gmsbank |
| p3 | P1 + the reviewed P2 peer columns gmsd, gmsm (182) | gmsbank + `/var/tmp/gmsbank/peer_gmsd` (pins in `p2_data.py`) |
| b1 | C7 f956-985 + dvifmgate f1820-1824 (35) | csfw_dvifm + restore |
| b1s | C7 with its five F1 columns (f956, f962, f968, f974, f980) replaced by dvifmgate f1820-1824 (30) | csfw_dvifm + restore |
| c8n | gmsbank f1322-1501 + gmsnative f1790-1819 (210) | gmsbank + restore |
| rall | restore ALL = A1 + B2 + B1 + C8n additions (533) | all |

The restore arms a1, a1m, b2, b2m (restore-cuts amendment) are unchanged. **A1w** (fold-specific drop of 60 R0 columns by the pinned label-free rule) is registered but its per-fold masking in the fit scripts is **not implemented yet**; it stays MISSING until it is, and is then added by dated note before any A1w result is read. P2 is the existing reviewed arm and is not re-run or modified here. The P1/P2/P3 interpretation table registered in the addendum stands.

## Conventions (all arms)

- **Controls:** the arm's added columns are permuted jointly within each reference over the non-identical `pair_key`s, seed 20260923 (identical-pair amendment). For P3 the six-plus columns (gmsbank block and peer pair) are permuted as **one** block with the same procedure, as in the registered P3 control.
- **Identical pairs:** fabricated zeros on `pixels_identical` keys for every added candidate/restore column (adapter-side, files unchanged). The reviewed P2 peer columns are **not** masked: P2 is a fixed, reviewed diagnostic control already run in its registered form, and P3 inherits it unchanged. Identical-key counts per set: kadid_train 39 keys (156 stimuli), kadid_select 25 (100), konfig_train 3, konfig_val 4, the other pinned sets 0.
- **Sign masks (superseding the restore amendment's "registry direction" sentence, before any restore result was read):** the run's BVLS uses the pinned legacy mask `feature_sign_mask_2026-05-26.tsv` (f0-f371 only); R0's own f372-f943 are unmasked. **Every added column is free**, for every arm, so an arm adds capacity symmetrically with R0's unmasked columns. (The staged adapter already left C1-C4 columns free.)
- **Constant columns:** the eight registered-stated-zero C1-C4 columns f1066-f1073 (gridblk (Y, scale 3)) are all-zero in every set; they stay in their arms as constants (the lasso/BVLS solvers already handle constants in R0).
- **Known property of the C3 inputs (REVIEW_PARTB item 3):** C3 p99 top-bin saturation exceeds the preregistered 1%-of-cells limit on four sets: kadid_train (ssim 0.53, art 2.87, det 2.88, mse 0.00 %), kadid_select (0.50, 3.26, 3.23, 0.00), tid2013 (0.17, 2.01, 2.01, 0.00) and kadid_terminal (0.47, 3.36, 3.32, 0.00; confirmation set, not used here); other sets <= 0.08 (ssim) and <= 0.02 (art, det); C1 passes everywhere. The sidecars are not changed; the exceedance is a calibration finding for the C3 owner, and C3-arm results on the three KADID/TID2013 sets are reported with this property noted. It is not a reason to skip or drop the arm (cost/quality clauses are removed from the bar).
- **Bar, statistics, seeds, sets, roles, forbidden sets, bootstrap (B=2,000, seed 20260923):** unchanged; D5 as amended (no cost clause). Cost is reported, never a gate.
- **Eras:** deterministic BVLS/lasso (D1, D2, stability) run locally, a dev-box era of their own. **Every MLP fit of every candidate/restore arm goes to the fleet AVX2 era**; none runs on the dev box.
