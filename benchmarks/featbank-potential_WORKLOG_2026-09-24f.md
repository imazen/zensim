# featbank-potential worklog f — Claude Sonnet takeover (2026-09-24)

POTENTIAL — ceiling, not a model score. Continued by Claude Sonnet lane featbank-potential, 2026-09-24T20:57:28Z. Role: run the preregistered fits and gates; no D5 or interpretation verdicts (Opus evaluator).

## 2026-09-24T20:57:28Z — takeover state check (read-only)
- cwd /home/lilith/work/zen/zensim--featbank-potential; bookmark quarantine/codex/featbank-potential at fc16a9fe0c56 (HANDOFF commit), working copy clean.
- Gate files: CODEX_QUOTA_STOP.md present (quota rule does not bind this lane); PARTB_C1C4_DONE.md present but a stop-report (0 sidecars, no candidate label readable); FLEET_FITS_READY.md / FLEET_FITS_GO.md / main_build binary_meta.json absent.
- Live processes: NO mlp_probe / accelerate_mlp / zensim_mlp_train / run-heavy fit runners from the Codex session. Five Codex-era waiter shells are alive (marker waiters, no fits): run_mlp_ci_after_grid (2198052), run_mlp_aggregate_after_core (2992134), run_finalize_after_aggregates (3256573), run_finalize_after_d2 (4021692), and a p2_report waiter (782872). They only fire on completion markers; left alone so nothing runs twice. Other lanes alive: claude-fleet-fits, claude-partb-c1c4 (its cargo build holds the heavy lock).
- Receipts at takeover (find result.json): P0 D1 MLP 44/960, P2 D1 MLP 0 by my find path (the Codex handoff says 25/960; my find pattern for p2 was wrong, recount below), combined D2 MLP 33 result.json under p2/d2_mlp*+lodo.
- /home 48 GB free (>=20 GB floor), /var/tmp 1158 GB free.

## 20:57–21:00 UTC — clean-main build, attempt 1 refused, re-snapshot
- Recount: P2 D1 MLP is 25/960 (my earlier find path was wrong); D2 MLP 31 under p2/d2_mlp.
- 20:58 UTC `~/tmp/devin/heavy --mem 24G --jobs 8 -- python scripts/rev4_featpot/build_main_diagnostic.py` (cwd = workspace; log `/var/tmp/rev4-featpot/main_build_compile_sonnet.log`) exited rc=1 after 4 s: the guard raised `zenjxl-decoder: main moved after snapshot b6948915... -> 623d0c36...`. No compile ran; legacy binaries untouched.
- 20:59 UTC `jj git fetch` in all 15 repos of `prepare_main_build.REPOS` (all rc 0, "Nothing changed"), moved the stale snapshot to `/var/tmp/rev4-featpot/main_build_20260924_stale_moved_zenjxl-decoder` (mv, not deleted), and re-ran `python scripts/rev4_featpot/prepare_main_build.py`. New `build_meta.json` sha256 `9db3b7c80d446f97f45ca317374bdab2e787dc91a8b569858874834932df296b`, 15 repos: zensim 6a7b88e5, zenmetrics ea16371c, zenjpeg 8f703a6e, zenwebp 8aa8a785, zenavif b5be33bd, zenjxl 4a2c021b, zenpng cfccd88f, zenanalyze b102fa5f, zenpixels e56f626b, jxl-encoder b16116ba, zenjxl-decoder 623d0c36, zenresize e3975fb9, zenbench 53941021, zenextras 109a9ec3, butteraugli aac925f6; overlay sha256 e4f4393c8a91...
- 20:59 UTC re-queued the build (log `main_build_compile_sonnet2.log`, pid file `main_build_sonnet.pid`); waits behind other lanes' heavy jobs.
- Staged (uncommitted until reviewed) `scripts/rev4_featpot/parity_audit_mlp.py`: re-trains the refit of two fixed completed cells (kadid_train r0 H32 o0_r0; konfig_val minus_basic H128 o1_r4) with the clean-main trainer into `/var/tmp/rev4-featpot/parity/mlp/` and compares every dumped checkpoint byte-for-byte to the pre-rule ones. Cells fixed before running.

## 21:04 UTC — cost-budget amendment (user directive via coordinator)
- Directive (2026-09-24): "remember not to reject things for the cost budget, and track all things and code and results of those you have. we can optimize and make things optional".
- Committed `benchmarks/rev4_featpot_cost_amendment_2026-09-24.md` (sha256 `3792fda530c15646bcb79dac1ef2a65ee1afc3240c5e21f24d5cd8ba135fceae`) as commit `61d3c07a050d` at 2026-09-24T21:04:58Z; bookmark `quarantine/codex/featbank-potential` moved forward to it. It removes plan §3 clause 3 ("within its §2.4 budget, measured") from the D5 bar for every arm (C1-C4, all-C, P1/P2/P3, later arms); cost is measured and reported per family, never used to accept or reject; every arm runs whatever its cost.
- Precondition checked before writing: 0 `features__rev4c1c4.parquet` sidecars, no candidate output dir under /var/tmp/rev4-featpot, so no candidate-arm result had been read. Commit message carries the What/Commands/Outputs/Numbers parts.

## 2026-09-24T22:11:24Z — clean-main build, parity audits (results so far)
- 21:17 UTC clean-main build succeeded (`build_main_diagnostic.py`, log `main_build_compile_sonnet2.log`): `binary_meta.json` sha256 `8cd147b3f2f9585934b6464646c4af33c8dfdbe7a6f150b8c22bf0ac022940ab`; bake_dial_refit 1ca9c574..., panel 278c601c..., zensim_mlp_train d3b98c1a...; legacy paths now symlink to them; pre-rule backups in `main_build_20260924/pre_current_main_binaries/` (bake_dial_refit a232a7e6..., panel 2fdb2cf5..., trainer 64bfd13c...).
- BVLS + lasso parity (`parity_audit_linear.sh`, konfig_train r0, log `parity/parity_linear.log`): result.json **bit-identical** for both (bvls 32b1790b..., linear d50fd79a...).
- MLP checkpoint parity (`parity_audit_mlp.py`, 2 fixed cells): all 24 checkpoints differ byte-wise (report sha256 a6ed8b39...). Control with the PRE-RULE trainer rerun on the same stored tables (`--control-pre-rule-trainer`): all 24 also differ from the stored originals (report sha256 0eba60de...). So byte-equality is not reproducible for this trainer on this box even with the same binary; not attributable to the current-main rebuild. Prediction-level comparison (`parity_compare_pred.py`, stored vs pre-rule rerun vs clean-main rerun: max |pred diff| and panel SROCC) was launched at ~21:5x UTC; result pending in `parity/parity_pred.log`.
- No verdict is drawn here; the Opus evaluator decides whether tolerance-level parity is acceptable and whether the existing MLP grid stays one era.

## 2026-09-24T22:11:24Z — csfw / C7 / B1 amendment
- Coordinator/user instruction: add arms R0+csfw and R0+C7 (each with permuted control) and a DVIFM curve-vs-gate pair if both forms exist, before reading any result. Committed `benchmarks/rev4_featpot_csfw_c7_amendment_2026-09-24.md` (sha256 `296a2358b20e9356704866e39c65c21172d7d9dcc7ae484efaf5e8ccb87869b9`) as commit `28daeec17d59` at 2026-09-24T22:11:24Z; bookmark moved. Precondition: 0 `features__csfw_dvifm.parquet` in the bank. B1 rule: run the pair only if both forms are identified as distinct column sets in the sidecar manifest, else MISSING.

## 2026-09-24T23:18:03Z — prediction-level MLP parity, and restart of the gated runners
- `parity_compare_pred.py` (heavy; log `parity/parity_pred.log`, report `/var/tmp/rev4-featpot/parity/parity_pred.json` sha256 `c98cb1cbf1a29d2477167625cf2e648fd56a373cd9272302dd70129e1acee718`): on the two fixed cells, predictions from the stored checkpoint, the pre-rule-trainer rerun and the clean-main-trainer rerun have **max |pred diff| = 0.0** in all three pairings, and identical panel SROCC/PLCC/KROCC (kadid_train r0 H32 o0_r0, n=1000: SROCC 0.9503887932972758, PLCC 0.9517274037748864; konfig_val minus_basic H128 o1_r4, n=109: SROCC 0.8761867680443014, PLCC 0.8819547457589335). The earlier byte differences are embedded checkpoint metadata: cmp of kadid ckpt_epoch025 shows the rerun is 8 bytes longer and its trailer differs in `timestamp_epoch`, `trainer_head_at_train` ("d9acc8e43157" vs "unknown") and `trainer_source_dir`. So byte-equality of checkpoints was an invalid criterion; the audit outcome is: BVLS/lasso result.json bit-identical, MLP predictions bit-identical on both cells. Scope: 2 MLP cells + 2 deterministic cells; no claim beyond those. The Opus evaluator decides whether this suffices to keep the grid one era.
- Runner gate: the shell runners hard-code the Codex quota file (`CODEX_QUOTA_STOP.md`, still present; the quota rule does not bind this lane). Made the path overridable (`${QUOTA_STOP_FILE:-...}`, default unchanged) in the 25 `run_*.sh`/resume scripts (`bash -n` clean) and launch with `QUOTA_STOP_FILE=/var/tmp/rev4-featpot/NO_QUOTA_GATE_SONNET_LANE` (nonexistent). The coordinator's file is untouched.
- Launched 2026-09-24T23:18:03Z, all under the shared heavy lock, no fleet GO (absent): `run_p2_deterministic.sh`, `run_p2_stability.sh`, `run_mlp_accelerator.sh` (P0 44/960, P2 25/960 at start; half P2/half P0 per 8-cell batch, P0 excluded automatically once FLEET_FITS_GO.md exists), `run_lodo_mlp_accelerator.sh` (D2 31/210), `run_lodo_jpeg.sh`. PIDs in `/var/tmp/rev4-featpot/sonnet_runner_pids.txt`; logs `/var/tmp/rev4-featpot/*_sonnet.log`. The five Codex-era waiter shells (CI/aggregate/finalize/P2-report) are still alive and untouched, so nothing runs twice.

## 2026-09-25T00:18:47Z — heartbeat and report refresh
- First restarted D2 batch (`p2/d2_mlp_accel/batch_0004.log`) exit 0 in 2660 s, D2 31 -> 39/210; P2 D1 deterministic 12/32; JPEG LODO contrasts complete; the P0/P2 D1 accelerator is queued on the heavy lock (P0 44/960, P2 25/960). FLEET_FITS_READY/GO absent; PARTB_C1C4_DONE.md still the stop report.
- `FEATBANK_POTENTIAL_DONE.md` got a status section (MISSING first, counts and recompute commands with actual output). Runners stay alive; the Codex-era waiter shells will run CI/aggregate/finalize when the grids complete — those finalizers may rewrite the report, so re-read it after them. Handoff: check `/var/tmp/rev4-featpot/sonnet_runner_pids.txt` and `*_sonnet.log`; on FLEET_FITS_GO.md let P0 cells finish at the boundary (accelerator/p0_fleet_pause.py handle it).

## 2026-09-25T10:22–10:33Z — FLEET_FITS_GO: local MLP runners paused
- Coordinator relayed `FLEET_FITS_GO.md` (written ~10:21Z): all potential-run MLP grids move to one AVX2 fleet era; dev-box AVX-512 MLP cells are diagnostic only; deterministic BVLS/lasso stays local; on harvest run `run_mlp_ci.sh`, `summarize.py`, `check_core_gates.py` on the harvested tree.
- 10:22Z: P0 already had no running process. Stopped my D1 accelerator shell (pid 20555) and its heavy-lock waiter (4193355; no cell had started), and the D2 accelerator shell (20562). The running D2 batch (batch_0012) had all 8 cells already selected (4 done, 4 mid-fit) and was left to finish; it exited rc=0 at 10:33Z (2583 s), D2 99 -> 103. `python p0_fleet_pause.py --runner handoff` wrote `FLEET_FITS_PAUSED.md` (76 P0 fit receipts; last cell POT_konfig_val_minus_basic_mlp32/o2_r0, sha256 48f062b4...). P2 deterministic (20547) and P2 stability (20550) keep running.
- Final local AVX-512 MLP cell counts: P0 76, P2 57, D2 103 = 236; manifest `/var/tmp/rev4-featpot/avx512_diagnostic_cells.tsv` sha256 `d7f1ef7c7a70120645453c161359a0091e29639d994d17622c39e373c89cbc8a`, every row era=avx512_devbox_DIAGNOSTIC_ONLY. The coordinator's "P0 108" is all result.json under fits/ (32 of them BVLS/lasso); one stale pilot dir without result (`fits/POT_aic3_r0_mlp128/o0_r0`, 2026-09-23 18:58) is not a receipt.
- Report status block rewritten for the new plan. Pending: when the fleet lane harvests (harvest_fit_cells.py --rescue-root), run `run_mlp_ci.sh`, `summarize.py`, `check_core_gates.py` on the harvested tree; nothing harvested yet (FLEET_FITS_DONE/STATUS not read here).

## 2026-09-25T15:5x–16:06Z — restore-cuts arms folded into the candidate prereg
- Coordinator: restore-cuts families landed on zensim main (impl `384d15e1`, ids + exposure ledger `abe694a0`): mapdev f1502-1561, z1max f1562-1789, gmsnative f1790-1819, dvifmgate f1820-1824; fold the reviewed proposal's arms into the candidate prereg, fix the A1w drop rule before any label read, MLP fits to the fleet AVX2 era, hand the cell specs to the fleet lane, no action on local AVX-512 MLP runs.
- Read the proposal from main (`jj file show -r main@origin`, copy sha256 `aaa0c239885d8afc08d33e57be19be675fa9d5e538a2eecd87a32da6fa4ada95`) and the registry layout (`feature_defs.rs`: slot id = base + (scale*3+channel)*per + local; z1max 12 cells x 19; B2m locals {3..9,13..18} x 12 = 156; A1m locals {0,1,2} x 12 = 36; C7 F1 columns f956+6*level).
- Commit `72b35b43439b` (2026-09-25T15:57:29Z): amendment `benchmarks/rev4_featpot_restore_cuts_amendment_2026-09-25.md` — arms A1, A1m, A1w, B2, B2m, B1, B1s, C8n, ALL with size-matched permuted controls (primary: joint within-reference permutation as in the C1-C4 amendment; the proposal's independent-column permutation registered as a secondary control for BVLS/lasso only); A1w drop rule fixed label-free (constant columns first, then the higher id of the highest-|r| pair, 60 total, per fit population); bootstrap kept at B=2000 seed 20260923 (proposal's 10,000/20260924 not used); no cost clause; MLP fits fleet-era only.
- Commit `c56290c10f13` (15:59:13Z, before any label read): `restore_arms_pin.py` (feature columns + manifests only; the admitted tables contain no label column) wrote the arms JSON (sha256 `6b920f2a...`, 18,459 B) and the 55 drop lists (sha256 `5addfec5...`, 14,685 B; split from the arms JSON because 32.7 KB exceeded the 30 KB record limit — correction recorded in `rev4_featpot_restore_arms_pin_2026-09-25.md`), plus a DATA_SPLITS.md exposure receipt.
- Commit `d96d33566267` (16:02:11Z): `restore_data.py` adapter (pin sha checks, join by pair_key, within-reference joint permutation control, seed 20260923) with `candidate_data.py` delegating arms a1, a1m, b2, b2m (and `_perm`), registry-direction BVLS sign masks for the packed columns (mapdev mse_dev and all z1max = pin_geq0, others free; extended TSV under /var/tmp/rev4-featpot/restore/, base mask file unchanged; `bvls_probe.fit` and `candidate_lodo` select it only for restore arms, so baseline fits are unaffected), and `run_restore_deterministic.sh`. Label-free smoke: side_join OK on all 9 pinned sets.
- 16:02:53Z started `run_restore_deterministic.sh` (pid file `sonnet_runner_pids.txt`, log `restore_deterministic_sonnet.log`; first attempt died on a missing log dir, fixed with mkdir -p, relaunched). First cell kadid_train a1 bvls finished 16:05:40Z (result sha256 `f50b70c6...`). No results interpreted.
- NOT runnable, recorded MISSING: A1w (fold-specific masking of the pinned drop lists is not implemented in the fit scripts), B1, B1s (C7 sidecar from Part B absent), C8n, ALL (C8/C7 sidecars absent). BVLS/lasso of a1/a1m/b2/b2m run locally; their MLP cells are specified for the fleet lane in `/home/lilith/tmp/zensim-paper/rev4/FEATBANK_POTENTIAL_RESTORE_CELLS_for_fleet.md` (D1 3,840 cells, D2 560 cells; unverified end-to-end, smoke cell first). I cannot message the fleet lane; the coordinator relays.

## 2026-09-25T16:28:18Z — identical-pair convention (coordinator decision on REVIEW_PARTB.md section 2)
- Decision: option (b), one convention = fabricated zeros on `pixels_identical` keys, adapter-side, no file change; option (a) (drop identical keys) recorded as the future-training recommendation, not applied (would change in-flight P0/P2 data).
- **Sequence problem, disclosed:** my local deterministic runner had already fitted the eight D1 `a1` BVLS cells (labels read, mapdev unmasked) and started `a1_perm` before this decision arrived; I had seen one cell's (kadid_train a1) SROCC log lines. Stopped the runner (pid 348290) and the queued job at 16:26–16:27Z; moved (not deleted) `candidates/d1` and its logs to `/var/tmp/rev4-featpot/candidates_VOID_mixed_identical_convention/` (8 result.json, sha256 prefixes in the amendment). VOID; never reported or compared.
- Amendment committed f569b10c21cd at 2026-09-25T16:27:40Z: `benchmarks/rev4_featpot_identical_pair_amendment_2026-09-25.md`.
- Adapter: `restore_data.apply_identical_convention` (flags from the admitted table's `pixels_identical`, consistency-checked per pair_key; zero-masks every added column on identical rows; perm controls permute jointly within reference over NON-identical pair_keys only, seed 20260923); `candidate_data.load` (C1-C4) now uses the same helper (it previously permuted over all rows; no C-arm result exists yet). Receipts carry `identical_convention` (identical rows/keys, added cells nonzero before mask).
- Label-free test (kadid_train, konfig_val, aic3 x a1, b2): identical rows all zero after masking in real and perm; per-reference multiset of non-identical rows preserved by the permutation; before-mask nonzero cells: kadid_train a1 7,488 = 156 rows x 48, konfig_val a1 192 = 4 x 48, b2 0 everywhere — matches REVIEW_PARTB.md.

## 2026-09-25T16:27–16:47Z — Part B promoted: candidate prereg finalised, adapters, cell specs, A1w
- Coordinator: Part B landed (zensim e6c9b630); C1-C4 / csfw_dvifm / gmsbank sidecars final under /var/tmp/rev4-featbank/bank/<set>/; finish the candidate prereg incl. the identical-key mask, build P1/P3/C-family/restore cell specs, hand them to the fleet lane; record C3 saturation.
- **Second void (disclosed):** after the identical-pair amendment I relaunched the restore runner; when I then changed the BVLS sign-mask policy (registry-direction -> all added columns free, in the Part B amendment) some restore cells had already run with the old masks. I killed the runner and its in-flight fit at 16:40Z and moved 18 result.json (plus logs) to `/var/tmp/rev4-featpot/candidates_VOID_registry_direction_masks/` without viewing any value. VOID. Cells now running started at 16:43Z or later under the final conventions (adapter `c9e7227bd711` and later).
- Commit `ff9df1b654c4` (2026-09-25T16:40:28Z): amendment `rev4_featpot_partb_arms_amendment_2026-09-25.md` + pin `rev4_featpot_partb_arms_2026-09-25.json` (sha256 9cfaecd4...; label-free `partb_arms_pin.py`: manifests, schemas, hashes, pixels_identical flags: kadid_train 39 keys/156 stimuli, kadid_select 25/100, konfig_train 3, konfig_val 4). Arms c1-c4, all, csfw, c7, p1, p3, b1, b1s, c8n, rall; all added columns sign-free; C3 p99 saturation recorded as a known property (kadid_train, kadid_select, tid2013, kadid_terminal); Codex candidate-pin flow superseded.
- Commit `c9e7227bd711` (16:43:15Z): `restore_data.py` rewritten (two pinned sources + peer join for p3; identical-pair convention with peer columns unmasked and the P3 control = P1 control + P2 control, recorded in erratum `rev4_featpot_partb_arms_erratum_2026-09-25.md`); `candidate_data` delegates all arm names; runner takes an arm list. Label-free tests: every Part B family is exactly 0 on identical rows before masking; mapdev 7,488 nonzero cells in kadid_train; peer pair on identical keys = gmsd 0.0 / gmsm 1.0.
- 16:43Z launched `run_restore_deterministic.sh` for 17 arms (c1 c4 c2 c3 all csfw c7 p1 p3 b1 b1s c8n rall a1 a1m b2 b2m; log `arms_deterministic_sonnet.log`). Commit `f5d26b14be95` (16:46:46Z): A1w implemented (dated note `rev4_featpot_a1w_note_2026-09-25.md`: population drop lists as coordinate slices for BVLS/lasso, `--keep-features` for MLP) and a second runner for arm a1w (`a1w_deterministic_sonnet.log`). Nothing interpreted.
- Fleet hand-off rewritten (`FEATBANK_POTENTIAL_RESTORE_CELLS_for_fleet.md`): 18 arms, 19,800 MLP cells (D1 17,280 + D2 2,520), weight 49,500 (H32=1, H128=4), spec sha256 `017529301f38...` from `arm_cell_specs.py` (enumeration only). No ETA claimed. MLP scripts untried for these arms; `rall` is 1,477 wide.
- NOT done / MISSING: lasso stability selection (`candidate_stability.py` not extended to the new arm names), MLP cells (fleet), every result interpretation.

## 2026-09-26T00:52–01:15Z — resume after plan-limit lift
- State at resume: all deterministic arm runs finished (both runners), P2 deterministic (32) and stability (16) done, Codex-era MLP runners still stopped, fleet running (fitp0, fitp0v7, fitp2, fitd2, then v8 leftovers).
- Commit `95bf3a7c`: `arms_summary.py` (mechanical; 756-row table behind a pointer file, 72-row brief table) and P2 report wording aligned with the no-cost-gate amendment; regenerated the P2 block by `p2_report.py`. Counts: 648 candidate result.json, 0 tracebacks; permuted control's own CI excludes zero in 80/252 D1-nested and 52/252 D2 (arm, model, set) cells (control-effect cells; instrument-insensitive by the registered rule). No verdict.
- Commit `c0e9fa52`: `candidate_stability.py` extended (added_all family; A1w slice = full-population drop list for all half-samples of a set) + `run_arms_stability.sh`; real-arm stability launched 00:57Z (order p1 p3 c7 csfw a1 a1w b2 a1m b2m c1-c4 all b1 b1s c8n rall; controls after). Pace not yet measured.
- **Harvest detected 01:07Z:** 281 fleet-era MLP cells now in my tree (P0 233 under fits/, P2 33, D2 15), identified by `fleet_receipt.json`; the 236 dev-box AVX-512 cells are still present and byte-identical to `avx512_diagnostic_cells.tsv`, so **the tree is mixed** (harvest is incremental; nothing was rescued away yet). run_mlp_ci.sh / summarize.py / check_core_gates.py need the complete grid and would mix eras, so they are NOT run now. Instead `fleet_follow.py` (pid in sonnet_runner_pids.txt, log fleet_follow.log) runs per-cell CIs (mlp_ci.py / p2_mlp_ci.py) only for cells with a fleet_receipt.json, and runs summarize.py + check_core_gates.py (into /var/tmp/rev4-featpot/fleet_era/) only once all 960 P0 cells are fleet-era. Dev cells never get a CI from it.

## 2026-09-26T01:34:06Z — follower env fix
- First follower attempt failed its first CI (`python: No such file or directory`: I had replaced the environment instead of extending it). Fixed `fleet_follow.py` to inherit `os.environ`; relaunched; the first fleet-era CI (P0 aic3 minus_basic H128 o4_r3) completed at 01:33Z (result `ci.json` sha256 897a9536...). Stability pace: first arm run (p1, kadid_train) 23 min wall / 584 s running; 144 real-arm runs queued, controls after. Handoff for whoever reads this next: the follower (`fleet_follow.py`), the real-arm stability runner and the fleet lane are the three live producers; PIDs in `/var/tmp/rev4-featpot/sonnet_runner_pids.txt`; the D5 bar needs stability results before any arm outcome.
