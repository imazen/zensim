//! Placeholder bench file — the actual Adam SIMD micro-benchmark
//! lives in a sibling worktree. This stub exists only to satisfy the
//! `[[bench]] name = "adam_bench"` entry in Cargo.toml during
//! mini-batch / parallel-batch development.

fn main() {
    eprintln!("adam_bench placeholder — see sibling worktree for full bench");
}
